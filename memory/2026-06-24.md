# 2026-06-24

## 진행 상황
- 회원관리 하위메뉴로 **크레딧 이력 관리** 및 **포인트 이력 관리** 메뉴를 추가하고, 이에 상응하는 조회/필터/엑셀 다운로드 기능 페이지를 개발 완료했습니다.
- **주요 구현 항목**:
  - `adminService.ts`: `getAllCreditTransactions` 및 `getAllPointTransactions` API 추가. 유저명, 병원명, 이메일, 아이디 검색 시 nested query 제약을 우회하기 위해 유저 ID 선조회 필터 방식을 적용.
  - `routes.tsx`: `/admin/credit-history` 및 `/admin/point-history` 경로 등록.
  - `AdminLayout.tsx`: '회원관리' 메뉴를 아코디언 형태로 변경하여 '회원 목록', '크레딧 이력 관리', '포인트 이력 관리' 3개의 서브 메뉴 노출 및 활성화 감지 로직 적용.
  - `CreditHistoryPage.tsx` & `PointHistoryPage.tsx`: 프리미엄 다크/블루 테마의 테이블 구현. 유형 필터링, 검색, 페이징 및 XLSX 라이브러리를 연동한 한글 엑셀 파일 다운로드 탑재.
- **빌드 테스트**: `npm run build`를 성공적으로 통과하여 컴파일 에러가 없음을 확인했습니다.
- **bkit PDCA**: `/pdca` 사이클에 맞추어 `docs/bkit/` 아래의 plan, task, check, act 문서를 최신 사양에 맞춰 모두 갱신했습니다.
