# PDCA Check: 회원관리 하위 크레딧 및 포인트 이력 관리 메뉴 추가와 조회 기능 구현

- [x] **라우팅 검증**: `/admin/credit-history` 및 `/admin/point-history` 경로가 정상적으로 동작하며 라우터에 바인딩되었는가?
- [x] **레이아웃 검증**: 데스크탑 화면에서 '회원관리' 클릭 시 서브메뉴(회원 목록, 크레딧 이력 관리, 포인트 이력 관리)가 올바르게 아코디언 형태로 노출되는가?
- [x] **API 검증**: `getAllCreditTransactions` 및 `getAllPointTransactions` 호출 시 각각의 트랜잭션 테이블과 `users` 테이블이 성공적으로 조인되어 데이터를 가져오는가?
- [x] **검색/필터 정합성**: 병원명/회원명 검색 및 거래 유형 필터링이 각각의 이력 화면에서 알맞게 필터링되어 반영되는가?
- [x] **페이지네이션 및 UI 정합성**: 하단 페이지네이션 및 단위 개수 변경이 테이블 데이터를 갱신시키는가?
- [x] **Excel 다운로드**: 필터링된 전체 내역이 한글 컬럼 포맷의 XLSX 파일로 깨짐 없이 다운로드되는가?
- [x] **빌드 정합성**: `npm run build` 결과 에러 없이 번들링이 완료되는가?
